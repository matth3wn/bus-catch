# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | API fallback strategy | Swiftly → public GTFS-RT → static schedule → mock | Graceful degradation ensures app always has some answer | No |
| D002 | M001 | pattern | Glanceable UX pattern | Full-screen color = answer, tap to expand detail | User wants <1s answer with zero interaction | No |
| D003 | M001 | scope | Route 222 southbound only | Single direction, single route for M001 | Focused scope; northbound deferred to future milestone | Yes — when northbound is added |
| D004 | M001 | arch | No backend/database | Client-side state only, API routes as proxy | Zero-config, no persistence needed for walk-or-wait | Yes — if features need persistence |
| D005 | M001 | convention | Data source indicator in state | `dataSource: 'realtime' \| 'schedule' \| 'mock'` field on BusCatchState | UI needs to show data confidence level for failure visibility | No |
| D006 | S01 | tech | Test framework | Vitest + happy-dom | Fast, native ESM/TS, path alias support — no config overhead for Next.js App Router | Yes |
| D007 | S01 | arch | Metro API v2 over raw GTFS-RT protobuf | Use `api.metro.net` JSON REST endpoints, skip protobuf entirely | Free, no API key, JSON format, eliminates protobuf dependency | No |
| D008 | S01 | arch | API response includes `source` field | Every prediction/vehicle response includes `source: 'swiftly' \| 'metro-realtime' \| 'schedule' \| 'mock'` | Enables downstream observability and data-quality awareness without inspecting payloads | No |
| D009 | S01 | pattern | Parse functions separated from fetch functions | `parseMetroTripDetail()` and `parseScheduleResponse()` are pure, exported separately from network-calling `fetch*()` functions | Enables deterministic testing with fixtures, no mocking of fetch needed for unit tests | No |
| D010 | S02 | convention | GPS thresholds: 50m accuracy, 100m off-route | `MAX_GPS_ACCURACY=50`, `MAX_OFF_ROUTE_DISTANCE=100` | Current 200m/500m values are no-ops for Cahuenga Blvd walking; 50m/100m are reasonable for urban GPS while still filtering noise | Yes — tune after S04 real-walk validation |
| D011 | S02 | convention | Staleness thresholds: warn at 60s, error at 120s | `STALENESS_WARNING_SECONDS=60`, `STALENESS_ERROR_SECONDS=120` | 15s poll interval means 4 consecutive failures before warning, 8 before error — avoids false positives from single missed polls | Yes — tune based on real-world API reliability |
| D012 | S02 | pattern | Source mapping: API source → BusCatchState dataSource | `'swiftly'/'metro-realtime'` → `'realtime'`, `'schedule'` → `'schedule'`, `'mock'` → `'mock'` | Simplifies D005's three-value enum; consumers don't need to know Swiftly vs Metro distinction | No |
| D013 | S02 | pattern | Extract speed estimation as pure function | `estimateSpeed()` in `lib/speed.ts` separate from hook | Makes speed calculation testable without rendering React components; same extraction pattern as D009 | No |
| D014 | S03 | tech | CSS-only transitions, no animation library | `transform: translateY()` + `transition-transform` for panel, `transition-colors` for background | One expand/collapse gesture doesn't justify framer-motion; zero bundle weight; GPU-accelerated | No |
| D015 | S03 | pattern | Dynamic theme-color via meta tag update | `useEffect` updates existing `<meta name="theme-color">` content attribute on recommendation change | PWA manifest theme_color is static; runtime meta tag override gives status bar matching color | No |
| D016 | S03 | pattern | Tap-only expand/collapse, no swipe gesture | `onClick` for expand, close button for collapse — no touch/swipe handlers | Avoids conflicts with detail panel scroll; simple tap is sufficient per D002 requirements | Yes — if user wants swipe-to-expand later |
| D017 | S04 | tech | Deploy to Vercel via GitHub integration | Push to GitHub, link repo to Vercel, auto-deploy | Zero-config for Next.js App Router; serverless API routes, edge caching, env var management all handled; `SWIFTLY_API_KEY` optional server-side env var | No |
| D018 | S04 | pattern | Keep hand-rolled service worker, no Workbox | `public/sw.js` with cache-first shell + network-first API | App has 1 page and 2 API routes — Workbox/Serwist adds complexity with no benefit; manual version bump (`bus-catch-v1`) sufficient for cache busting | Yes — if app scope grows significantly |
| D019 | S04 | pattern | Remove hardcoded manifest link, rely on Next.js auto-injection | Delete `<link rel="manifest" href="/manifest.json" />` from layout.tsx | `app/manifest.ts` exists → Next.js auto-generates `/manifest.webmanifest` and injects the `<link>` tag; hardcoded link causes 404 and blocks PWA install | No |
| D020 | S04 | infra | Deploy via Vercel CLI with GitHub auto-link | `vercel --yes --prod` with GitHub repo `matth3wn/bus-catch` | CLI auto-detected Next.js, linked GitHub repo for auto-deploys on push; Vercel project: `matthew-8149s-projects/bus` | No |
| D021 | S04 | config | No SWIFTLY_API_KEY in production | App runs on Metro API v2 + schedule fallback only | Swiftly key not available; fallback chain (D001) works correctly — `source: "mock"` when no live GTFS-RT data | Yes — add key via Vercel dashboard when available |
| D022 | M002/S01 | arch | Reverse existing southbound polyline for northbound route | `NORTHBOUND_WALKING_ROUTE = [...WALKING_ROUTE].reverse()` | Same sidewalk area, <20m difference between sides of street; well within 100m off-route threshold; avoids sourcing a separate GTFS shape | Yes — if snapping precision issues arise |
| D023 | M002/S01 | pattern | Direction detection via net latitude displacement | `detectDirection()` returns northbound/southbound/null from ≥3 samples with >30m net displacement | Cahuenga Blvd runs roughly north-south; latitude is the dominant axis; simple and testable as a pure function | Yes — tune threshold after real-walk data |
| D024 | M002/S01 | arch | API routes accept `?direction=` query param | Default to `direction=1` (southbound) for backward compatibility | Clean parameterization; client sends direction from detected state; no breaking change to existing API contract | No |
| D025 | M002/S02 | pattern | Compact direction badge on glanceable screen | "↑ NB" / "↓ SB" text in translucent pill next to data source badge | Minimal visual footprint; direction only shown once detected (null = hidden); doesn't compete with main recommendation | Yes — if users want more prominent direction display |
